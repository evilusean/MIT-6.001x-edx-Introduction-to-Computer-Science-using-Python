data = []
file_name = input("Provide a name for file for data")
try:
  fh = open(file_name, 'r')
except IOError:
  print("can not open", file_name)
else:
  for new in fh:
    if new != '/n':
      addIt = new[:-1].split(',')
      data.append(addIt)
finally:
  fh.close()
